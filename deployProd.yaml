apiVersion: apps/v1
kind: Deployment
metadata:
  name: ReactForm
  namespace: dev
  labels:
    app: Form
spec:
  replicas: 1
  selector:
    matchLabels:
      app: Form
  template:
    metadata:
      labels:
        app: Form
    spec:
      containers: 
      - image: 244740641779.dkr.ecr.us-east-1.amazonaws.com/team2:latest
        name: Form
        imagePullPolicy: Always 
 
        ports:
        - containerPort: 8000

        resources:
          requests:
            memory: "500Mi"
            cpu: "500m"
          limits:
            memory: "500Mi"
            cpu: "500m"
